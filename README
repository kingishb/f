File finder. 

Install:
cargo build --release

Usage:
    # find files with the extension xml starting at cwd
    f \.xml$

    # find files with the extension xml starting at ~/bin
    f --root ~/bin \.xml$


License:
MIT
